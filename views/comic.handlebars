<h2>
    {{text}}
</h2>

{{#each comic}}

<div class="card">
    <div class="card-body comics" data-id={{_id}}>

        <div class="card-title">
            <h5>
                {{comic}}
            </h5>
        </div>

        <img class="card-img-bottom images" src={{img}} alt={{comic}}>
    </div>
</div>
<br>

{{/each}}

<button class="btn btn-info" id="retrieve">Get Today's Comics</button>

<script>

    function loading(button) {
        const newDiv = $("<div>").addClass("spinner-border text-primary spinner")
        const newSpan = $("<span>").addClass("sr-only").text("Loading...")
        newDiv.append(newSpan)

        $(button).replaceWith(newDiv)
    }

    let selection = localStorage.getItem("selection");


    $(document).on("click", "#retrieve", function () {
        loading("#retrieve")

        fetch("/scrape", function (data) {

        })
        setTimeout(function () {
            location.reload();

        }, 3000);
    })

    $(document).on("click", ".card-body", function () {
        const id = $(this).data("id");
        window.location = `/comic/${id}`
    })
</script>